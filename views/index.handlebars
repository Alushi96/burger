<div class="container">
    <div class="row">
        <div class="col-md-6">
            <h2>Burgers</h2>
            <ol>
                {{#each burgers}}
                    {{#unless devoured}}
                    <li class="order">
                        {{burger_name}}
                    </li>
                        <button class="devour" data-id="{{id}}" data-devoured="1">Devour</button>
                    {{/unless}}
                {{/each}}
            </ol>
        </div>
        <div class="col-md-6">
            <h2>Devoured Burgers</h2>
            <ol>
                {{#each burgers}}
                    {{#if devoured}}
                    <li class="order">
                        {{burger_name}}
                    </li>
                    {{/if}}
                {{/each}}
            </ol>
        </div>
    </div>
    <div class="row">
        <div class="col-md-12">
            <form class="create-form">
                <h2> Add A Burger</h2>
                <div class="form-group">
                    <label for="bg">Burger Name</label>
                    <input type="text" id="bg" name="burger_name">
                </div>
                <button type="submit">Add Burger</button>
                <p>Enter the name of the burger you would like to eat and click Add Burger!</p>
            </form>
        </div>
    </div>
</div>